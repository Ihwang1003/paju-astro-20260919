# 파주 관측 노트 프로젝트

2026-09-19 파주 안시관측 세션을 위한 대상 목록 웹페이지를 생성하는 프로젝트.

## 파일 구조

- `data.py` — 관측 세션 정보(SESSION)와 62개 대상 데이터(CATEGORIES). 각 항목 필드:
  - `name_kr`/`name_en`/`cat`(분류)/`const`(별자리)
  - `mag`(밝기 텍스트)/`mag_num`(정렬용 숫자, 이중성은 주성 등급)
  - `size`/`dist`/`sep`(이중성 각거리, 있을 때만)
  - `season`(일반적인 적정 관측 시기, 텍스트)
  - `visual`(1~5, 안시관측 추천도 — 등급뿐 아니라 표면밝기·크기·형태까지 고려한 주관적 평가)
  - `astro`(촬영추천 등급: best/ok/hard/visual) + `astro_note`
  - `note`(관측 포인트 설명), `link`(위키백과 링크), `flag`(선택, 경고문구)
  - `ra`/`dec`(J2000 좌표, 십진 시/도) 또는 `win`(태양계 천체용 수동 관측가능시간 오버라이드 (시작시, 종료시) — 20:00을 0시로 하는 시간축)
- `astro.py` — 파주(위도 37.76N, 경도 126.78E) 기준 GMST/고도 계산 유틸. 2026-09-19 20:00 ~ 09-20 06:00(KST) 세션 동안 각 대상의 "오늘 밤 관측 가능 시간대"(고도 20° 이상 기준)를 실제로 계산.
- `build.py` — data.py + astro.py를 읽어 `output.html` 하나를 생성하는 빌드 스크립트. 카드형 UI, 필터(유형/안시추천도/촬영추천도)와 정렬(기본/곧 지는 순/안시추천도순/밝은순/가나다순) JS 포함.
- `output.html` — 빌드 결과물. 이 파일 자체가 최종 산출물이며 브라우저로 바로 열면 됨.

## 실행 방법

```bash
python3 build.py
```
→ 같은 폴더에 `output.html`이 (재)생성됨. 이후 브라우저로 열어 확인.

## 참고사항

- 세션 날짜/장소/달 위상/일몰·박명 시각 등은 `data.py`의 `SESSION` 딕셔너리에 있음. 날짜를 바꾸려면 이 값과 `astro.py`의 `SESSION_START`/`SESSION_END`, 그리고 태양계 천체(달/행성)의 수동 `win` 값들을 다시 계산해서 넣어야 함(딥스카이 좌표는 그대로 재사용 가능).
- 고도 임계값(현재 20°)은 `astro.py`의 `ALT_THRESHOLD_DEG`.
- 원래 이 프로젝트는 Claude(Cowork) 클라우드 세션에서 Artifact 도구로 게시했음. 로컬(Claude Code)에서는 게시 기능이 없으므로, 대신 결과물인 `output.html`을 그냥 브라우저로 열거나 원하면 Netlify/GitHub Pages 등에 직접 올리면 됨.
- 사진은 외부 이미지를 직접 삽입하지 않고 위키백과 링크로 대체되어 있음(원 세션의 네트워크 제약 때문). 로컬 환경은 이미지 다운로드가 자유로우므로, 원한다면 각 카드에 실제 썸네일 이미지를 추가하는 작업을 여기서 이어갈 수 있음.
